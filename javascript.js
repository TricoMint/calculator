function add(num, num2) {
    let add;
    add = num + num2;
    return add
}

function subtract(num, num2) {
    let subtract;
    subtract = num - num2;
    return subtract
}

function multiply(num, num2) {
    let multiply;
    multiply = num * num2;
    return multiply;
}

function divide(num, num2) {
    let divide;
    divide = (num / num2);
    return divide;
}